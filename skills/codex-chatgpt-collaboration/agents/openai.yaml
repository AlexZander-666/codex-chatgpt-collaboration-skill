interface:
  display_name: "Codex × ChatGPT Collaboration"
  short_description: "Consult visible top-tier ChatGPT before each task"
  default_prompt: "Use $codex-chatgpt-collaboration before starting this task: reuse ChatGPT, verify the current visible Pro or strongest labeled tier, submit exactly once, obtain complete advice, then execute and verify locally."

policy:
  allow_implicit_invocation: true
